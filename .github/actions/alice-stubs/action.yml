name: 'ALICE-Bridge Optional Bridge Stubs'
description: |
  No-op composite action for ALICE-Bridge (0 sibling path deps).
  Kept as canonical placeholder to preserve template symmetry across ALICE-* crates.

  Canonical CI template: [[reference_alice_ci_canonical_template]]

  license = "AGPL-3.0-or-later" — 自己 exception を deny.toml で明示
  今回は sibling path dep 0 のため stub 生成不要 (no-op)

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        set -euo pipefail
        echo "ALICE-Bridge has 0 sibling path deps; alice-stubs is a no-op."
